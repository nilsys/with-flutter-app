import 'package:flutter/material.dart';

class SelectStoryView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Select Story'),
      ),
    );
  }
}
